{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start",
    "clean": "lerna clean --yes",
    "lint": "lerna run --stream lint"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
